/**
 * Interface representing a map with its dimensions.
 *
 * @interface IMap
 */
export interface IMap {
  /**
   * The width of the map.
   *
   * @type {number}
   */
  width: number;

  /**
   * The height of the map.
   *
   * @type {number}
   */
  height: number;
}
